"use client";import{useRouter}from"next/navigation";export function StickyHeader({title="",fallbackHref="/"}:{title?:string;fallbackHref?:string}){let r=useRouter();return <header className="fixed inset-x-0 top-0 z-50 bg-zinc-950 p-4"><div className="mx-auto flex max-w-2xl"><button onClick={()=>history.length>1?r.back():r.push(fallbackHref)}>← Zpět</button><b className="mx-auto">{title}</b></div></header>}